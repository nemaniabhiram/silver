export function App() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-[640px] flex-col items-center justify-center px-6 text-center">
      <h1 className="text-display">Drop it. It's live.</h1>
      <p className="mt-4 text-body text-text-dim">
        Drag a folder or a .zip — your static site goes live in seconds.
      </p>
    </main>
  );
}
